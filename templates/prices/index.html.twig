{% extends 'base.html.twig' %}

{% block title %}Prices index{% endblock %}

{% block body %}
    <h1>Prices index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for price in prices %}
            <tr>
                <td>{{ price.id }}</td>
                <td>{{ price.prix }}</td>
                <td>
                    <a href="{{ path('app_prices_show', {'id': price.id}) }}">show</a>
                    <a href="{{ path('app_prices_edit', {'id': price.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_prices_new') }}">Create new</a>
{% endblock %}
